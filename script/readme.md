# Tracker Script

- This logs every 5 seconds the app and/or website that is currently focused. 
- This exe will log to ~/AppData/Local/tracker_data/
- Windows only

# Instructions
To compile into an exe, run `go build -ldflags "-H windowsgui" -o tracker.exe`
